- **2026-06-29**: Core Engine Phase 1 execution completed. Handled idempotency deduplication, HMAC-SHA256 signature verification for webhooks, and Supabase client setup. Fixed token race condition with promise deduplication. Cached admin client at module level to optimize connections. Set route handler runtime to Node.js for crypto availability.

- **2026-07-02**: Customer Portal (Phase 4b) built end-to-end against the live Go API. Added `/pay` route group: access page (magic-link token exchange + enumeration-safe email request form), subscription home (status badge, ₦ amount, renewal date, cancel-at-period-end dialog, past_due banner), invoice list + detail with line items, payment-method page (cardless virtual-account panel first, card-on-file stub pending Track A's Nomba client). Plumbing: regenerated `lib/api/v1.d.ts` from `dev-utils/openapi.yaml` (added missing `/portal/logout` to the contract), openapi-fetch client now sends credentials, TanStack Query provider, `formatDate`/`intervalLabel` helpers, new ui primitives (input, card, skeleton, dialog) + StatusBadge. Fixed missing `tailwindcss-animate` dependency that broke the build. Verified in a real browser (headless Brave): magic-link sign-in, all screens, cancel flow, consumed-token replay rejected, access-request confirmation.

- **2026-07-02**: Landing page built at `/` (replaced the create-next-app placeholder). Static server component: nav, hero with mock portal-card visual (cardless "transfer detected → renewed" strip), 6-feature grid (cardless renewal, revenue split, fault tolerance, portal, RLS, observability), 3-step how-it-works, violet CTA band, footer. Primary CTAs → `/signup` (dashboard, pending), secondary → `/pay/access` (live). Verified via headless-browser screenshots at 1280px and 390px.

- **2026-07-05**: Frontend auth Phase 1 — switched from cookie auth to Bearer tokens (frontend on Vercel and API on CloudFront are cross-site, so cookies get dropped). Added `lib/auth/token.ts`: SSR-safe localStorage store for two independent session tokens (`tenant` dashboard, `portal` customer). Reworked `lib/api/index.ts`: removed `credentials: "include"`; added an openapi-fetch `onRequest` middleware that attaches `Authorization: Bearer <token>` — portal token for `/portal/*` routes, tenant token otherwise. Typecheck clean.

- **2026-07-05**: Frontend auth Phase 2 — wired the dashboard login/signup (were stubs that just `router.push`'d). `login` and `signup` pages now call `api.POST('/auth/login'|'/auth/signup')`, store the returned session token via `setTenantToken`, surface API errors, then navigate to `/overview`. Added `components/dashboard/auth-guard.tsx` (client): runs `useUser()` (`GET /me` with the Bearer token) and `router.replace('/login')` on 401; wrapped the dashboard `(main)` layout's content in `<AuthGuard>`. Token response field read via cast pending Phase 4 contract/type regen. Whole project typechecks clean.

- **2026-07-05**: Frontend auth Phase 3 — portal Bearer auth + card-field fix. `useExchangeToken` now stores the returned portal session token via `setPortalToken` (so `/portal/*` calls authenticate cross-site without a cookie); `usePortalLogout` clears it (`clearPortalToken`, onSettled). Fixed `useSaveCard` to send `nomba_token_key` (was `tokenized_card`, which the backend rejects) and updated the payment-method page call site to match. Typechecks clean.

- **2026-07-05**: Frontend auth Phase 4 — contract/types sync + cleanup. Added optional `token` to the `Tenant` and `PortalContext` schemas in `dev-utils/openapi.yaml` (present only on signup/login/portal-session responses), regenerated `lib/api/v1.d.ts` (only +2 lines — types were otherwise in sync). Dropped the now-unneeded casts: token reads are `res?.token`/`ctx?.token`; removed `(api as any)` on `/portal/virtual-account` and `/portal/payment-method/card` (paths are typed). Uninstalled unused `@supabase/ssr` + `@supabase/supabase-js`. `next build` passes (15 routes, TS clean).
